<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/app_navigation"
    app:startDestination="@id/login">

    <fragment
        android:id="@+id/nav_comms"
        android:name="com.and.newton.comms.CommsHomeFragment"
        android:label="@string/menu_communication"
        />
    <fragment
        android:id="@+id/login"
        android:name="com.and.newton.login.Login"
        android:label="Login" >
        <action
            android:id="@+id/action_login_to_nav_comms"
            app:destination="@id/nav_comms" />
    </fragment>
</navigation>